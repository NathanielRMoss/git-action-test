name: Node Continuous Integration

on:
   workflow_dispatch:

jobs:
  test_pull_request:
    runs-on: ubuntu-latest
    steps: 
      - run: 'yarn' 
        working-directory: ./simpleapp
      - run: 'yarn build'
        working-directory: ./simpleapp
